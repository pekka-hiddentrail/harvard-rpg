{
  "name": "@harvard/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/browser.tsx",
  "comment": "No dependency on @harvard/engine, deliberately. The client holds zero game rules — it asks the server what a build costs (ARCHITECTURE §4).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --project tsconfig.browser.json --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/react": "^16.3.3",
    "@testing-library/user-event": "^14.6.6",
    "@types/react-dom": "^19.2.5",
    "@vitejs/plugin-react": "^6.1.1",
    "jsdom": "^29.1.1",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  }
}
